# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# gem "rails"

# native jekyll
# gem "jekyll", "~> 4.2"

# jekyll that contains essential gh themes
gem "github-pages", group: :jekyll_plugins

gem "webrick", "~> 1.7"
